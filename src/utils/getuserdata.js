export default  function getuserdata(type,uuid) {
  let obj={
    'msg':true,
    'data':'',
    'err':'',
  }
  if(type===''||uuid===''){
    obj.msg=false
    return obj
  }else{
    switch(type){
      
    }
  }
}